apiVersion: platform.local.lab/v1alpha1
kind: Api
metadata:
  name: authorized-api
spec:
  parameters:
    namespace: platform-connections-demo
    image: ghcr.io/cujarrett/platform-connections-demo-downstream:sha-ceccc7248015507745a9dc70b0b5f411c77476b5
    repo: https://github.com/cujarrett/platform-connections-demo
    port: 8080
    replicas: 1
    connectionPosture: enforce
    # Reaching upstream-api needs nothing here — that call is granted on upstream-api
    # itself. Only the off-platform destination has to be registered.
    provides:
      - name: demo
        allowedCallers:
          - { namespace: platform-connections-demo, app: connections-demo }
    consumes:
      - { host: api.open-meteo.com }
