apiVersion: platform.local.lab/v1alpha1
kind: Api
metadata:
  name: neon-toaster-api
spec:
  parameters:
    namespace: guest-neon-toaster
    image: ghcr.io/cujarrett/hello-world-api:latest
    port: 8080
    replicas: 1
    host: demo3-api.mattjarrett.dev
    tlsSecret: demo3-api-tls
    sqlRef:
      name: neon-toaster-sql
      backend: private-cloud
    nosqlRef:
      name: neon-toaster-nosql
    objectStorageRefs:
      - name: neon-toaster-store
    cache:
      enabled: true
      backend: private-cloud
    readinessCheckPath: /readyz
