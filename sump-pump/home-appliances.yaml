apiVersion: platform.local.lab/v1alpha1
kind: Topic
metadata:
  name: home-appliances
  namespace: sump-pump
spec:
  parameters:
    streamName: HOME-APPLIANCES
    subjects:
      - "home.appliance.>"
    retention: limits
    maxAge: "720h"
