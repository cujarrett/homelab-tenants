apiVersion: platform.local.lab/v1alpha1
kind: Api
metadata:
  name: weather-exporter
spec:
  parameters:
    namespace: sump-pump
    image: ghcr.io/cujarrett/weather-exporter:sha-f85647fe251d8445b97e0c336717027a45b1bf69
    repo: https://github.com/cujarrett/weather-exporter
    port: 8080
    metricsPort: 8080
    # The only call it makes. Nothing else on the internet is reachable.
    consumes:
      - { host: api.open-meteo.com }
